<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodel="clr-namespace:Maintain_it.ViewModels"
             xmlns:fontawesome="clr-namespace:Maintain_it.Helpers.FontAwesome"
             x:Class="Maintain_it.Views.TestView">
    <ContentPage.BindingContext>
        <viewmodel:TestViewModel/>
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <StackLayout>
            <Label Text="TESTING" 
                   FontSize="Title" TextColor="Red"
                   HorizontalOptions="Center"/>
            <Frame>
                <StackLayout>
                    <Entry Text="{Binding Name}"/>
                    <Entry Text="{Binding NextNodeId}" Keyboard="Numeric"/>
                    <Entry Text="{Binding PreviousNodeId}"/>
                </StackLayout>
            </Frame>

            <Frame>
                <StackLayout>
                    <Label Text="{Binding DbName}"/>
                    <Label Text="{Binding DbNextNodeId}"/>
                    <Label Text="{Binding DbPreviousNodeId}"/>
                </StackLayout>
            </Frame>

            <Button Text="Notify" Command="{Binding NotifyCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
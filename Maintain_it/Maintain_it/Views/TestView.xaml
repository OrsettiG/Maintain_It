<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodel="clr-namespace:Maintain_it.ViewModels"
             xmlns:fontawesome="clr-namespace:Maintain_it.Helpers.FontAwesome"
             x:Class="Maintain_it.Views.TestView">
    <ContentPage.BindingContext>
        <viewmodel:TestViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="TESTING" 
                   FontSize="Title" TextColor="Red"
                   HorizontalOptions="Center"/>
            <Frame BackgroundColor="Gray">
                
                <CollectionView ItemsSource="{Binding TestData}" BackgroundColor="LightGray">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Grid BackgroundColor="LightBlue">
                                <Grid.RowDefinitions>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="4*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.GestureRecognizers>
                                    <DragGestureRecognizer
                                        DragStartingCommand="{Binding DragStartingCommand}"
                                        DragStartingCommandParameter="{Binding Name}"/>
                                    <DropGestureRecognizer
                                        DragLeaveCommand="{Binding DragLeaveCommand}"
                                        DragLeaveCommandParameter="{Binding Name}"
                                        DragOverCommand="{Binding DragOverCommand}"
                                        DragOverCommandParameter="{Binding Name}"
                                        DropCommand="{Binding DropCommand}"
                                        DropCommandParameter="{Binding Name}"/>
                                </Grid.GestureRecognizers>
                                <Label Grid.Column="0" WidthRequest="50" 
                                       Text="{Static fontawesome:FontAwesomeIcons.GripLines}" FontFamily="FA-Solid"
                                       FontSize="Large"
                                       HorizontalTextAlignment="Center" VerticalTextAlignment="Center"/>
                                <Label Grid.Column="1" Text="{Binding Name}" FontSize="Large"/>
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            
            </Frame>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>